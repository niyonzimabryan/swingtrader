You are building Strategy Lab PR 4 for the SwingTrader Investment Workspace (repo niyonzimabryan/swingtrader). Work on branch `claude/strategy-lab-4-integration` from `main`. Nothing needs the owner; Bryan is asleep. Commit and push to your branch after each coherent unit (not only at the end) so a usage-limit interruption loses nothing; if interrupted and resumed, run `git status` first and continue from what is pushed.

Read first, in this order: `AGENTS.md` (the four non-negotiables), `specs/investment-workspace/strategy-lab/strategy-lab-architecture.md` (§3, §6, §9, §11, §15, §16, §19), `specs/investment-workspace/strategy-lab/strategy-lab-agent-prompts.md` (Agent 4 block and the shared rules), `docs/STRATEGY_LAB.md` (all sections, PR 1–3), `strategy_lab/` as it stands (PR 1 domain/registry; PR 2 snapshots/snapshot_builder/execution_policy/indicators/validation/strategies; PR 3 runner/shadow/replay/metrics and `scripts/strategy_lab_scoreboard.py`), `orchestrator/pipeline.py` (`TradingPipeline`), `orchestrator/scheduler.py`, `bot/telegram_bot.py`, `bot/handlers/commands.py`, `bot/handlers/callbacks.py`, `bot/handlers/performance.py`, `bot/weekly_report.py`, `config/settings.py` (the existing flag families, e.g. `research_workspace_enabled`, `phase6_execution_enabled`), `.env.example`, `docs/ENV_SETUP.md`, `tracking/shadow_ledger.py` (the legacy ScoredCandidate ledger), and `todoscratchpad.md`'s Strategy Lab entries.

GOAL — Agent 4 of the plan: default-off shadow integration with the current scans plus owner-only Telegram visibility and controls. Prerequisites PRs 1, 2 and 3 are merged on `main`.

Scope ownership: a narrow hook in `orchestrator/pipeline.py`, scheduler configuration/jobs in `orchestrator/scheduler.py`, bot handlers/keyboards/weekly report under `bot/`, the flag family in `config/settings.py` and `.env.example`, `docs/ENV_SETUP.md` (a new §10 for the Strategy Lab flags), and shadow-only integration/E2E tests. Do not touch `execution/`, `portfolio/`, `comparables/`, `workspace/`, or `research_workspace/`. Another session may concurrently be finishing `claude/evidenced-budget-closure` in `comparables/`, `portfolio/evidence.py`, `research_workspace/store.py` and `workspace/tools.py` — do not modify those files.

Requirements (the Agent 4 block):
1. With every new flag false, prove current scans, memos, paper auto-approval and broker paths behave identically: zero new writes, zero output changes, asserted by test with the existing pipeline fixtures.
2. With shadow enabled (`STRATEGY_LAB_ENABLED` and `STRATEGY_LAB_SHADOW_ENABLED`, both default false), build one snapshot after the existing analysis and run all active shadow arms through PR 3's runner. A Strategy Lab failure must never block existing memo delivery — isolate it, log it, continue.
3. Continue the legacy `ScoredCandidate` ledger unchanged during migration.
4. Owner-only `/experiments`, `/strategies`, `/strategy <slug>`, pause/resume commands and a weekly scoreboard section (reuse `scripts/strategy_lab_scoreboard.py`'s renderer; do not reimplement the numbers). Exclude promote and live-kill commands — their safety services are PR 5/6.
5. Display samples, maturity, costs, drawdown, benchmark, uncertainty, correlation and warnings. Recommendations are informational only; nothing in a Telegram message is a number a model produced.
6. Funnel/log events with no sensitive payloads.
7. Tests for duplicate scans, restart, DB failure isolation, Telegram authorization (a non-owner is refused), empty data, partial results, and long-duration pending positions.
8. Build cross-sectional snapshots once per rebalance cutoff, not once per ticker.

Acceptance: a mocked full scan produces the existing memo plus N strategy decisions; disabling the flags produces zero new writes and zero output changes; no broker method is called by shadow mode under any tested failure path (assert with a fake broker that raises on any call).

Standing rules: every flag defaults off and is documented in `.env.example` and `docs/ENV_SETUP.md`; do not change production Railway variables or scheduled-job timings that exist today; no broker order; no new migration unless genuinely needed (branch it from the current single head `0010_merge_execution_lifecycle` if so; the orchestrator joins heads); no model-client, broker, or network import in `strategy_lab/`; never weaken an existing test; `mcp` stays `<2`; no secrets. Validate with `python -m compileall -q .` and `python -m unittest discover -s tests -p "test_*.py"` (3 Postgres-only skips are expected on SQLite). Push and open a PR against `main` titled "Strategy Lab PR 4: default-off shadow integration and owner-only Telegram controls (Spec Q §9, §11)" whose body lists the contract items, the flags added, what was exercised and what was not, and anything deferred with its reason. The orchestrating session merges it; you do not.
