__pycache__/
*.py[cod]
*.db
*.db-journal
robinhood_token.enc
robinhood_token.enc.tmp
.env
.env.local
.env.*
!.env.example
*.egg-info/
dist/
build/
.venv/
venv/
.idea/
.vscode/
*.log
.DS_Store
# `.claude/` is local session state, except the subagent briefs, which are
# repo files on purpose: Spec P §4 defines them once so Codex and Cursor can
# mirror them, and tests/test_agent_layer.py parses the committed copies.
.claude/*
!.claude/agents/
!.claude/skills/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
# `data/` is both a source package (the vendor adapters) and, historically, a
# runtime scratch directory. Ignore its contents by default and re-include the
# source that lives there: without this, new modules under data/ are silently
# untracked (the existing ones predate the rule and are tracked already).
/data/*
!/data/*.py
!/data/prices/
sessions/
/sessions/
cache/
caches/
.Codex/
