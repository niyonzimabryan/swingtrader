anthropic>=0.79.0
python-telegram-bot>=20.0
sqlalchemy>=2.0
pydantic-settings>=2.0
alpaca-py>=0.21.0
yfinance>=0.2.36
lxml>=5.0
fredapi>=0.5.1
finnhub-python>=2.4.18
aiohttp>=3.9
apscheduler>=3.10
structlog>=24.0
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.2
pandas>=2.0
numpy>=1.24
langfuse>=3.0
opentelemetry-instrumentation-anthropic>=0.1
google-genai>=1.0
firecrawl-py>=2.0
mcp>=1.27.2,<2  # 2.x renamed streamablehttp_client -> streamable_http_client; breaks execution/brokers/robinhood.py and scripts/robinhood_auth.py
cryptography>=42,<47
arch>=8.0,<9  # Spec N §6.1/§7: stationary block bootstrap, optimal_block_length (Politis-White 2004 + PPW 2009) and StepM. Tested against 8.0.0; capped below the next major because a major bump can rename the bootstrap API, and an unbounded pin was already a live bug once (mcp 2.x).
statsmodels>=0.14.2,<0.16  # Spec N §6.2: two-way clustered (event date x ticker) covariance via OLS cov_type='cluster', cov_kwds={'groups': 2-column array}. Tested against 0.15.0; floor and cap are a deliberate narrow window because cov_kwds is the one API here with no stability guarantee.
