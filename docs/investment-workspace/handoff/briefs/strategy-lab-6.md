You are building Strategy Lab PR 6 for the SwingTrader Investment Workspace (repo niyonzimabryan/swingtrader). Work on branch `claude/strategy-lab-6-tournament` from `main`. Commit and push to your branch after each coherent unit (not only at the end) so a usage-limit interruption loses nothing; if interrupted and resumed, run `git status` first and continue from what is pushed.

Read first, in this order: `AGENTS.md` (the four non-negotiables), `specs/investment-workspace/strategy-lab/strategy-lab-architecture.md` (§3, §9, §11, §12, §13, §15, §16, §19 — the definition of done), `specs/investment-workspace/strategy-lab/strategy-lab-agent-prompts.md` (Agent 6 block and the shared rules), `docs/STRATEGY_LAB.md` (all sections, PR 1–5), `docs/EXECUTION_LIFECYCLE.md`, `docs/ENV_SETUP.md`, `strategy_lab/` as it stands (PR 1 domain/registry; PR 2 snapshots/SDK/strategies; PR 3 runner/shadow/replay/metrics + `scripts/strategy_lab_scoreboard.py`; PR 4 the pipeline hook, flags and Telegram controls; PR 5 `strategy_lab/execution.py`, the explicit-mode entry point on Phase 6's `execution/lifecycle.py::ExecutionService`), `portfolio/killswitch.py`, `portfolio/approvals.py`, `execution/brokers/fake.py` (`FakeExecutionBroker`), `bot/handlers/commands.py` and the PR 4 handlers, `docs/investment-workspace/handoff/HANDOFF.md`, and `todoscratchpad.md`'s Strategy Lab entries.

GOAL — Agent 6 of the plan: the complete disabled-by-default Strategy Lab — paper tournament, audited promotion, full documentation, end-to-end proof, and a production-safe rollout plan. No live order, no production flag change.

Scope ownership: the paper-arm dispatcher through PR 5's explicit-mode entry point, `strategy_lab/promotion.py`, the final owner-only Telegram promote/live-kill controls (extend PR 4's handlers; `/live_kill` already exists from Phase 6 — reuse it, do not add a second switch), end-to-end tests, docs (README, PRD, `docs/SYSTEM_CAPABILITIES.md`, operator runbook, `docs/ENV_SETUP.md`, architecture diagrams), and release verification only.

Requirements (the Agent 6 block):
1. Dispatch eligible paper arms through PR 5's explicit-mode entry point, reusing its placement internals. Pass immutable `mode=paper` and the Alpaca paper adapter. Never call the current global-mode entry point for a Strategy Lab arm. Reject any mismatch before broker review or placement. Enforce independent virtual budgets, caps, no duplicate orders, and experiment tags.
2. Owner-only, confirmed, append-only promotions and demotions (`promotion_events`, PR 1). The system can recommend; it cannot auto-promote. Authorizations bind the source-evidence arm and snapshot plus a separate inactive target arm with the same immutable strategy version, requested mode and risk budget; reject evidence reuse for a different target. Live activation atomically replaces the one global champion (`uq_experiment_arms_single_live_champion`) or leaves the prior champion unchanged on failure.
3. Require a complete evidence snapshot (`experiment_metric_snapshots`) and warning acknowledgement for tier changes.
4. Live promotion requires PR 5's capability checks, all feature flags, kill-switch clearance, current risk gates, and owner confirmation. Promotion is not entry approval: each proposed live execution needs its own signed, expiring, owner-only callback — reuse Phase 6's `portfolio/approvals.py` (mint/verify/single-use) rather than a second signing scheme; it atomically approves only the referenced proposed execution id and rejects replay, stale, wrong-owner and state drift.
5. Execute the complete E2E fixture: scan → snapshot → four arms → shadow results → paper orders (fake Alpaca paper) → closures → metrics → owner report → promotion dry run.
6. Update README, PRD, SYSTEM_CAPABILITIES, operator runbook, environment docs, and architecture diagrams. State exactly what remains disabled.
7. Run full CI plus an independent pre-landing review (the `code-review` pass over your own diff). Fix every P0/P1.
8. Draft but do not execute the production rollout checklist: migrations, shadow flag, observation window, paper flag, Robinhood review-only canary, and separately authorized micro-live canary.

Acceptance: Section 19's definition of done is satisfied or each unmet item is explicitly blocked with evidence; existing behaviour is unchanged when all Strategy Lab flags are false; no live trade occurred and production capital/settings were untouched; a safety E2E sets the global execution mode to live while dispatching a paper arm and proves only the Alpaca paper fake is called and the live broker sees zero calls; the final handoff gives exact commands/results, PRs/commits, rollback, and the next owner decision.

Standing rules: every flag defaults off; no new migration unless genuinely needed (branch from the current single head — check `alembic heads`); never weaken an existing test; the workspace import closure must still reach no `execution/`; no model-client import on any execution path; `mcp` stays `<2`; no secrets. Validate with `python -m compileall -q .` and `python -m unittest discover -s tests -p "test_*.py"` (3 Postgres-only skips expected on SQLite; CI runs Python 3.12). Push and open a PR against `main` titled "Strategy Lab PR 6: paper tournament, audited promotion, and the release proof (Spec Q §13, §19)" whose body lists the contract items against §19, what was exercised and what was not, and anything deferred with its reason. The orchestrating session merges it; you do not.
